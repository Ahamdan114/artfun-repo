import { Card } from "@/components/Card";

export default function Home() {
    return (
        <div style={{height: "1000px"}}> // This is just to make the page scrollable
            <h1>Home</h1>
            <Card />
        </div>
    );
}
